# Environment variables for docker-compose (local development)
# Replace the placeholder values before running `docker-compose up`.

# OMDb API key (MovieService.Api)
OMDB_API_KEY=your_omdb_api_key_here

# OpenAI API key (RecommendationService.Api / Semantic Kernel)
OPENAI_API_KEY=your_openai_api_key_here

# JWT settings (UserService & ApiGateway)
JWT_KEY=change_this_to_a_long_random_secret
JWT_ISSUER=FlickBinge
JWT_AUDIENCE=FlickBingeClients

# Optional: override SQL SA password (the compose file currently uses Pass@word)
# MSSQL_SA_PASSWORD=Pass@word

# Optional: any other per-environment overrides
# e.g. alternate ports, feature flags
#EXTRA_SETTING=value
